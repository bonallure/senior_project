import React from "react";

const Input = ({id, type}) => {
    return(
        <>
            <input
                id = {id}
                type = {type}
            />
        </>
    );
};

export default Input;